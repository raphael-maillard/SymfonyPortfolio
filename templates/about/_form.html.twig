{{ form_start(form) }}
    {# {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button> #}
    <div class="container">
            <div class="form-group mb-2 ">
                {{ form_label(form.Title,'Titre') }}
                {{ form_widget(form.Title, {'attr': {'class': 'form-control col-md-12'}}) }}
            </div>  

            <div class="form-group mb-2 ">
                {{ form_label(form.resume,'resume') }}
                {{ form_widget(form.resume, {'attr': {'class': 'form-control col-md-12'}}) }}
            </div>

            <div class="form-group mb-2 ">
                {{ form_label(form.imageFile,'imageFile') }}
                {{ form_widget(form.imageFile, {'attr': {'class': 'form-control col-md-8'}}) }}
            </div>  

            <button class="btn btn-success m-1">{{ button_label|default('Sauvegarder') }}</button>
    </div>

{{ form_end(form) }}
